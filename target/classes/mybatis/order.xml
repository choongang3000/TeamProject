<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="student.model.STUOrder">
  
	<insert id="InsertOrder">
		insert into orders(onum, aid, totprice, odate) 
		values(ord_seq.nextval, #{aid}, #{totprice} ,sysdate)
	</insert>
	
	<select id="GetMaxOnum" resultType="int">
		select max(onum) from orders where aid=#{aid}
	</select>
	
	<select id="OrderList" resultType="student.model.STUOrderBean">
		select * from orders where aid=#{aid} order by onum desc
	</select>
	
	<select id="OrderAll" resultType="student.model.STUOrderBean">
		select * from orders order by onum desc
	</select>
</mapper>
